package com.example.gerimedicaexam.utils;

public class IntUtils {
    static public Integer parseInt(String source) {
        if (source.isEmpty())
            return  null;
        return Integer.parseInt(source);
    }
}
